{
  "name": "contributorops",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --workspace @contributorops/api\" \"npm run dev --workspace @contributorops/web\"",
    "build": "npm run build --workspace @contributorops/api && npm run build --workspace @contributorops/web",
    "build:all": "npm run build --workspace @contributorops/api && npm run build --workspace @contributorops/web && npm run site:build",
    "daily": "npm run daily --workspace @contributorops/api",
    "hourly": "npm run hourly --workspace @contributorops/api",
    "start": "npm run start --workspace @contributorops/api",
    "test:api": "npm run test --workspace @contributorops/api",
    "security:audit:runtime": "npm audit --omit=dev --audit-level=high",
    "security:audit": "npm audit --audit-level=high",
    "security:sbom": "npm sbom --sbom-format cyclonedx",
    "growth:quality": "node scripts/growth-integrity.mjs",
    "site:dev": "npm run dev --workspace @contributorops/site",
    "site:build": "npm run build --workspace @contributorops/site",
    "site:preview": "npm run preview --workspace @contributorops/site",
    "site:quality": "npm run site:build && node scripts/site-quality.mjs && npm run growth:quality",
    "site:lighthouse": "npm run site:build && node scripts/prepare-lighthouse.mjs && npx --yes @lhci/cli@0.15.0 autorun --config=./lighthouserc.json",
    "site:check": "npm run site:quality",
    "typecheck": "cd apps/api && npx tsc --noEmit && cd ../web && npx tsc --noEmit && cd ../site && npx tsc --noEmit",
    "ci": "npm ci && npm run test:api && npm run build:all && node scripts/site-quality.mjs && npm run growth:quality"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
